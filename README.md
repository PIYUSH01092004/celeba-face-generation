# celeba-face-generation
A vanilla GAN built in PyTorch to generate 64x64 RGB faces using the CelebA dataset. It features deep fully connected layers for the generator and discriminator, BCELoss, and Adam optimizers. Designed as a structural baseline for adversarial deep learning before upgrading to convolutional models.
